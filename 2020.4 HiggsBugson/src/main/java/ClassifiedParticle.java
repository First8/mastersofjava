
public class ClassifiedParticle extends Particle {
    public ClassifiedParticle(String id, String name, float mass, float charge, float spin, Type type) {
        super(id, name, mass, charge, spin, type);
    }
}
