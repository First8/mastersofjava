/**
 * represents the outcome of a ballot card analysis. 
 */
public enum Result {

    /** indicates a blank vote. */
    BLANK,
    /** indicates a vote for to Stay. */
    STAY,
    /** indicates a vote for to Exit. */
    EXIT,
    /** indicates an invalid ballot card. */
    INVALID;
    
}
